import React from 'react';

const Categories = () => {
  const button = <button type="button">Check status</button>;
  return (
    <div>
      {button}
    </div>
  );
};

export default Categories;
